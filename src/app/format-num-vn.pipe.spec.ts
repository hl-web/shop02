import { FormatNumVNPipe } from './format-num-vn.pipe';

describe('FormatNumVNPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNumVNPipe();
    expect(pipe).toBeTruthy();
  });
});
